<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>User</title>
</head>
<body>
  <h1>List of User:</h1>
  <ul>
    <% users.forEach(user => { %>
        <li>
            <ul>
                <h3><%= user.id %></h6>
                <h2><%- user.name %></h1>
                <!-- só troque pelo outro pra estar correto -->
                <!-- <h1><%= user.name %></h1> -->
            </ul>
        </li>
    <% }); %>
  </ul>
</body>
</html>
